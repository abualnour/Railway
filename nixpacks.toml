[phases.setup]
aptPkgs = [
    "python3.12",
    "python3.12-venv",
    "python3.12-dev",
    "python3-pip",
    "libcairo2-dev",
    "pkg-config",
    "libpango1.0-dev",
    "libgdk-pixbuf2.0-dev",
    "libglib2.0-dev",
    "libfontconfig1-dev",
    "libfreetype6-dev",
    "libffi-dev",
    "postgresql-client",
]

[phases.install]
cmds = [
    "python3 -m venv /opt/venv && . /opt/venv/bin/activate && pip install -r requirements.txt"
]